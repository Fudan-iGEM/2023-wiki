image: node:16.20-alpine3.17

pages:
  cache:
    paths:
      - node_modules/
  script:
    - echo start building
    - npm install
    - npm run docs:build
  artifacts:
    paths:
      - public
  only:
    - dev
